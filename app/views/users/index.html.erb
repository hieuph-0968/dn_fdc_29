<% provide :title, t(".all") %>
<div class = "container" >
  <h1><%= t(".all") %></h1>
  <ul class="users">
    <% @users.each do |user| %>
      <li>
        <%= gravatar_for user %>
        <%= link_to user.name, user %>
      </li>
    <% end %>
  </ul>
  <%= will_paginate %>
</div>
